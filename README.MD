# node-postgres-mysql-api

## Project setup
```
npm install
```

### Run on development mode using nodemon
```
npm run dev
```

### Commands in order to run a  docker container
```
docker-compose up -d mysql
docker-compose up -d phpmyadmin
```
